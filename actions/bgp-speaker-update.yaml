---
description: Update BGP Speaker's information.
enabled: true
entry_point: src/wrapper.py
name: bgp-speaker-update
parameters:
    base:
        default: bgp-speaker-update
        immutable: true
        type: string
    cloud:
        description: A specific cloud to query
        type: string
    ep:
        default: <cliff.commandmanager.EntryPointWrapper object at 0x2ebbd10>
        immutable: true
        type: string
    id:
        description: ID or name of bgp_speaker to update.
        required: true
        type: string
    name:
        description: Name of the BGP speaker to update.
        type: string
    project_id:
        description: Run the action under a different project, identified by id
        type: string
    project_name:
        description: Run the action under a different project, identified by name
        type: string
    request_format:
        description: '==SUPPRESS== (choices: json)'
        type: string
runner_type: python-script
