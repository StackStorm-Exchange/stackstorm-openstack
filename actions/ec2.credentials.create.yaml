---
description: Create EC2 credentials
enabled: true
entry_point: src/wrapper.py
name: ec2.credentials.create
parameters:
    base:
        default: ec2 credentials create
        description: null
        immutable: true
        required: false
        type: string
    column:
        default: []
        description: specify the column(s) to include, can be repeated
        immutable: false
        required: false
        type: array
    ep:
        default: EntryPoint.parse('ec2_credentials_create = openstackclient.identity.v2_0.ec2creds:CreateEC2Creds')
        description: null
        immutable: true
        required: false
        type: string
    format:
        default: json
        description: 'the output format, defaults to table (choices: html, json, shell,
            table, value, yaml)'
        immutable: false
        required: false
        type: string
    max-width:
        default: 0
        description: Maximum display width, 0 to disable
        immutable: false
        required: false
        type: integer
    prefix:
        default: ''
        description: add a prefix to all variable names
        immutable: false
        required: false
        type: string
    project:
        description: !!python/unicode 'Specify an alternate project (default: current
            authenticated project)'
        immutable: false
        required: false
        type: string
    user:
        description: !!python/unicode 'Specify an alternate user (default: current
            authenticated user)'
        immutable: false
        required: false
        type: string
    variable:
        default: []
        description: ==SUPPRESS==
        immutable: false
        required: false
        type: array
runner_type: run-python
