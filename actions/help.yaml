---
    name: help
    runner_type: run-python
    description: help from the openstack client.
    enabled: true
    entry_point: src/wrapper.py
    parameters:
        positional:
            type: string
            default: --help
            immutable: true
        format:
            type: string
            default: default
            immutable: true
