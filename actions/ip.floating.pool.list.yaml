---
description: List floating-ip-pools
enabled: true
entry_point: src/wrapper.py
name: ip.floating.pool.list
parameters:
    base:
        default: ip floating pool list
        description: null
        immutable: true
        required: false
        type: string
    ep:
        default: EntryPoint.parse('ip_floating_pool_list = openstackclient.compute.v2.floatingippool:ListFloatingIPPool')
        description: null
        immutable: true
        required: false
        type: string
    format:
        default: json
        description: 'the output format, defaults to table (choices: csv, html, json,
            table, yaml)'
        immutable: false
        required: false
        type: string
runner_type: run-python
