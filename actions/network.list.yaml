---
description: List networks
enabled: true
entry_point: src/wrapper.py
name: network.list
parameters:
    base:
        default: network list
        description: null
        immutable: true
        required: false
        type: string
    column:
        default: []
        description: specify the column(s) to include, can be repeated
        immutable: false
        required: false
        type: array
    dhcp:
        description: DHCP agent ID
        immutable: false
        required: false
        type: string
    ep:
        default: EntryPoint.parse('network_list = openstackclient.network.v2.network:ListNetwork')
        description: null
        immutable: true
        required: false
        type: string
    external:
        default: false
        description: List external networks
        immutable: false
        required: false
        type: boolean
    format:
        default: json
        description: 'the output format, defaults to table (choices: csv, html, json,
            table, yaml)'
        immutable: false
        required: false
        type: string
    long:
        default: false
        description: List additional fields in output
        immutable: false
        required: false
        type: boolean
    max-width:
        default: 0
        description: Maximum display width, 0 to disable
        immutable: false
        required: false
        type: integer
    quote:
        default: nonnumeric
        description: 'when to include quotes, defaults to nonnumeric (choices: all,
            minimal, none, nonnumeric)'
        immutable: false
        required: false
        type: string
runner_type: run-python
