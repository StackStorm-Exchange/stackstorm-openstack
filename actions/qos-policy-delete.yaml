---
description: Delete a given qos policy.
enabled: true
entry_point: src/wrapper.py
name: qos-policy-delete
parameters:
    base:
        default: qos-policy-delete
        immutable: true
        type: string
    cloud:
        description: A specific cloud to query
        type: string
    ep:
        default: <cliff.commandmanager.EntryPointWrapper object at 0x2ebf990>
        immutable: true
        type: string
    id:
        description: ID(s) or name(s) of policy to delete.
        required: true
        type: string
    project_id:
        description: Run the action under a different project, identified by id
        type: string
    project_name:
        description: Run the action under a different project, identified by name
        type: string
    request_format:
        description: '==SUPPRESS== (choices: json)'
        type: string
runner_type: python-script
