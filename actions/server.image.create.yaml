---
description: Create a new disk image from a running server
enabled: true
entry_point: src/wrapper.py
name: server.image.create
parameters:
    base:
        default: server image create
        description: null
        immutable: true
        required: false
        type: string
    column:
        default: []
        description: specify the column(s) to include, can be repeated
        immutable: false
        required: false
        type: array
    ep:
        default: EntryPoint.parse('server_image_create = openstackclient.compute.v2.server:CreateServerImage')
        description: null
        immutable: true
        required: false
        type: string
    format:
        default: json
        description: 'the output format, defaults to table (choices: html, json, shell,
            table, value, yaml)'
        immutable: false
        required: false
        type: string
    max-width:
        default: 0
        description: Maximum display width, 0 to disable
        immutable: false
        required: false
        type: integer
    name:
        description: !!python/unicode 'Name of new image (default is server name)'
        immutable: false
        required: false
        type: string
    prefix:
        default: ''
        description: add a prefix to all variable names
        immutable: false
        required: false
        type: string
    server:
        description: !!python/unicode 'Server (name or ID)'
        immutable: false
        required: true
        type: string
    variable:
        default: []
        description: ==SUPPRESS==
        immutable: false
        required: false
        type: array
    wait:
        default: false
        description: !!python/unicode 'Wait for image create to complete'
        immutable: false
        required: false
        type: boolean
runner_type: run-python
