---
    name: server.show
    runner_type: run-python
    description: server show
    enabled: true
    entry_point: src/wrapper.py
    parameters:
        base:
            type: string
            default: server
            immutable: true
        verb:
            type: string
            default: "show {{id}}"
            immutable: true
        id:
            type: string
            required: true
        format:
            type: string
            default: json
            immutable: true
