---
description: Display service details
enabled: true
entry_point: src/wrapper.py
name: service.show
parameters:
    base:
        default: service show
        description: null
        immutable: true
        required: false
        type: string
    catalog:
        default: false
        description: !!python/unicode 'Show service catalog information'
        immutable: false
        required: false
        type: boolean
    column:
        default: []
        description: specify the column(s) to include, can be repeated
        immutable: false
        required: false
        type: array
    ep:
        default: EntryPoint.parse('service_show = openstackclient.identity.v2_0.service:ShowService')
        description: null
        immutable: true
        required: false
        type: string
    format:
        default: json
        description: 'the output format, defaults to table (choices: html, json, shell,
            table, value, yaml)'
        immutable: false
        required: false
        type: string
    max-width:
        default: 0
        description: Maximum display width, 0 to disable
        immutable: false
        required: false
        type: integer
    prefix:
        default: ''
        description: add a prefix to all variable names
        immutable: false
        required: false
        type: string
    service:
        description: !!python/unicode 'Service to display (type, name or ID)'
        immutable: false
        required: true
        type: string
    variable:
        default: []
        description: ==SUPPRESS==
        immutable: false
        required: false
        type: array
runner_type: run-python
