---
description: List user-role assignments
enabled: true
entry_point: src/wrapper.py
name: user.role.list
parameters:
    base:
        default: user role list
        description: null
        immutable: true
        required: false
        type: string
    column:
        default: []
        description: specify the column(s) to include, can be repeated
        immutable: false
        required: false
        type: array
    ep:
        default: EntryPoint.parse('user_role_list = openstackclient.identity.v2_0.role:ListUserRole')
        description: null
        immutable: true
        required: false
        type: string
    format:
        default: json
        description: 'the output format, defaults to table (choices: csv, html, json,
            table, yaml)'
        immutable: false
        required: false
        type: string
    max-width:
        default: 0
        description: Maximum display width, 0 to disable
        immutable: false
        required: false
        type: integer
    project:
        description: !!python/unicode 'Filter users by <project> (name or ID)'
        immutable: false
        required: false
        type: string
    quote:
        default: nonnumeric
        description: 'when to include quotes, defaults to nonnumeric (choices: all,
            minimal, none, nonnumeric)'
        immutable: false
        required: false
        type: string
    user:
        description: !!python/unicode 'User to list (name or ID)'
        immutable: false
        required: false
        type: string
runner_type: run-python
